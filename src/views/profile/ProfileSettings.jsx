import React from 'react';
import { Settings } from 'component/index';

const ProfileSettings = () => {
	return (
		<div className="h-full">
			<Settings />
		</div>
	);
};

export default ProfileSettings;
