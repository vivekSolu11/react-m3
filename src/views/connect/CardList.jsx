import CardList from 'component/connect/connectCard/CardList';

const Connect = () => {
	return (
		<main className="h-[calc(100vh-82px)]  pb-10 overflow-y-auto overflow-hide">
			<CardList />
		</main>
	);
};

export default Connect;
