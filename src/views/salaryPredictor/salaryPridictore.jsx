import SalaryForm from 'component/salaryPridictore/SalaryForm/SalaryForm';

const Salary = () => {
	return (
		<main className="h-full overflow-y-auto overflow-hide mb-6 flex flex-col">
			<SalaryForm />
		</main>
	);
};

export default Salary;
